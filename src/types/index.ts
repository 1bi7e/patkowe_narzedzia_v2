/** Jedyne dwie użytkowniczki aplikacji — wybór profilu zamiast logowania. */
export type Stylistka = 'patrycja' | 'agata'

/** Wszystkie kwoty w aplikacji trzymamy w groszach (integer, nigdy float). */
export type Grosze = number
